networks:
  doctrine.net:
    driver: bridge

services:
  api.doctrine:
    build:
      context: build
      dockerfile: 8.4-cli.Dockerfile
    ports:
      - '6080:80'
    volumes:
      - ./app:/var/php
    working_dir: /var/php
    networks:
      - doctrine.net
    depends_on:
      - prati.db
    command: sh -c "composer install && php -S 0.0.0.0:80 -t /var/php/public"

  prati.db:
    image: 'postgres:latest'
    env_file: ./env/prati.env
    ports:
      - '5432:5432'
    networks:
      - doctrine.net
    volumes:
      # - ./sql:/var/sql
      - ./sql/prati:/docker-entrypoint-initdb.d/

  adminer:
    image: adminer
    ports:
      - '8080:8080'
    networks:
      - doctrine.net

